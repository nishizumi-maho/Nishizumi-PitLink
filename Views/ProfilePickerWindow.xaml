<Window x:Class="NishizumiPitLink.Views.ProfilePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose a Pit House preset for this car" Height="480" Width="620"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" Margin="0,0,0,4"
                   Text="MOZA Pit House Motor presets — edit them in Pit House itself; this app always reads the latest version"/>
        <TextBox Grid.Row="1" x:Name="SearchBox" Margin="0,0,0,6" TextChanged="SearchBox_TextChanged"
                 ToolTip="Search by name or wheelbase..."/>

        <ListBox Grid.Row="2" x:Name="PresetList" DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Use" Width="90" IsDefault="True" Click="Use_Click"/>
        </StackPanel>
    </Grid>
</Window>
